import 'package:flutter/material.dart';

class Widget1 extends StatelessWidget {
  const Widget1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Widget 1', style: TextStyle(fontSize: 24.0));
  }
}
